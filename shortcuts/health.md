Check my Mac's health right now.

Do this:
- Call `mac_health`.

The tool ALREADY returns two sections: "## Status" and "## NEEDS ATTENTION". The
NEEDS ATTENTION verdict is computed by the system itself — it is authoritative.

Show me BOTH sections. Relay the "## NEEDS ATTENTION" section EXACTLY as the tool
gave it — do not add, remove, soften, or invent any alerts, and do not re-judge the
numbers yourself. Just present it. You may tidy the Status formatting, but keep all
the actual figures unchanged.
